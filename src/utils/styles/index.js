/**
 * @flow
 * styles > index.js
 * Export variables and grid as whole
 */
import * as vars from './variables';
import * as gridStyles from './grid';

// Export variables
export const { SCHEME, COLORS, FONT } = vars;

// Export Grid
export const { Page, Grid, Section } = gridStyles;
